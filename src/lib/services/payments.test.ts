import { describe, it } from "vitest";

describe("payment method response contract", () => {
  it.todo(
    "throws when the API returns succeeded=false instead of converting the response to an empty list",
  );
});
